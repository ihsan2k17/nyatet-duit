import LoadingComponent from '@/shared/ui/components/effect/loading'

const Loading = () => {
    return (
        <div>
            <LoadingComponent />
        </div>
    )
    }

export default Loading
